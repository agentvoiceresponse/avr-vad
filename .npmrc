# Disable optional dependencies to avoid GPU-related installation issues
optional=false

# Environment variables for onnxruntime-node
export ORT_DISABLE_DNNL=1
export ORT_TENSORRT_UNAVAILABLE=1
export ONNXRUNTIME_PREFER_CPU=1

# Prevent downloading GPU binaries
prefer-offline=false
prefer-online=true 